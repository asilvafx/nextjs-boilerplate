const Footer = () => {
    return (
        <footer className="footer">
            © {new Date().getFullYear()} Starlit Tarot — gentle readings • privacy respected
        </footer>
    );
};

export default Footer;
